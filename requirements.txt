# Sensor Studio
#
#     pip install -r requirements.txt
#
numpy
pyserial

# and, for the window itself.
# PySide6 is the only thing here that needs a recent macOS: the current
# release is built for macOS 13 and newer. On macOS 12 install
# PySide6==6.9.3 first, on macOS 11 PySide6==6.7.3, then run this file.
PySide6
pyqtgraph

# optional. Sensor Studio says what is missing and runs without them.
bleak
scipy
# imufusion has no pre-built package for Windows on ARM or for Python 3.15,
# and pip would try to compile it there and fail. So it is left out on those:
# the numpy attitude filter in studio/core.py is used instead, and --check
# says so.
imufusion; platform_machine != "ARM64" and python_version < "3.15"
